<Window x:Class="LRDT.UjRendelesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LRDT"
        mc:Ignorable="d"
        Title="Új rendelés - LRDT" Height="200" Width="200" ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="0.5*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="1.5*" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.ColumnSpan="3" Text="Asztal" Grid.Row="0" VerticalAlignment="Bottom" Margin="20px 0px"/>
        <ComboBox x:Name="cbElerhetoAsztalok" Grid.ColumnSpan="3" Grid.Row="1" VerticalAlignment="Top" Margin="20px 5px" SelectionChanged="cbElerhetoAsztalok_SelectionChanged"></ComboBox>

        <Button x:Name="btnUj" IsEnabled="False" Content="Új rendelés" Grid.Row="2" Grid.Column="1" Margin="5px" Click="btnUj_Click" />
        <Button x:Name="btnVissza" Content="Vissza" Grid.Row="2" Grid.Column="2" Margin="5px" Click="btnVissza_Click" />
    </Grid>
</Window>
