<Window x:Class="LRDT.RendelesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LRDT"
        mc:Ignorable="d"
        Title="RendelesWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="0.5*" />
            <RowDefinition Height="2*"/>
            <RowDefinition Height="0.5*"/>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="8*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="0.5*"/>
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Text="Asztal" Margin="10px 0px" />
        <ComboBox Margin="10px" Grid.Row="1" />

        <TextBlock Grid.Row="2" Text="Rendelés" Margin="10px 0px" />
        <ComboBox Grid.Row="3" Margin="10px" IsEnabled="False" />
        <Button Grid.Row="3" Grid.Column="1" Margin="10px" Content="Új rendelés" IsEnabled="False" />
    </Grid>
</Window>
